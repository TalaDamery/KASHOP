@model List<Category>

@if (!Model.Any())
{
    <div class="alert alert-warning">No category Found</div>
}

else
{
    <table class="table table-bordered" role="table">
        <thead>
            <tr>
                <th style="width: 10px" scope="col">#</th>
                <th scope="col">Name</th>
                <th>Action</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var category in Model)
            {
                <tr class="align-middle">
                    <td>@category.Id</td>
                    <td>@category.Name</td>
                    <td>
                        <a class="btn btn-danger" asp-controller="categories" asp-action="remove" asp-route-id="@category.Id">
                            Delete
                        </a>
                        <a class="btn btn-info" asp-controller="categories" asp-action="edit" asp-route-id="@category.Id">
                            Edit
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
<div class="card-footer clearfix">
    <ul class="pagination pagination-sm m-0 float-end">
        <li class="page-item"><a class="page-link" href="#">«</a></li>
        <li class="page-item"><a class="page-link" href="#">1</a></li>
        <li class="page-item"><a class="page-link" href="#">2</a></li>
        <li class="page-item"><a class="page-link" href="#">3</a></li>
        <li class="page-item"><a class="page-link" href="#">»</a></li>
    </ul>
</div>

